let path,express,test;_35f‍.w("path",[["default",["path"],function(v){path=v}]]);_35f‍.w("express",[["default",["express"],function(v){express=v}]]);_35f‍.w("./src/test",[["test",["test"],function(v){test=v}]]);// const path = require('path')
// const express = require('express')





const app = express() //,
          // DIST_DIR = __dirname,
          // HTML_FILE = path.join(DIST_DIR,'index.html')
const port = 9000

// app.use(express.static(DIST_DIR))
app.get('/', (req, res) => {
   res.send(test)
  // res.send(add(1,2))
  // res.sendFile(HTML_FILE)
  // hello()
})

app.listen(port, () => {
  console.log(`Example app listening on port ${port}`)
})